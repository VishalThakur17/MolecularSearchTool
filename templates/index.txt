<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Molecular Search Tool</title>
  <style>
    :root {
      --bg: #f7fbff;
      --bg-2: #eef7ff;
      --panel: rgba(255, 255, 255, 0.96);
      --panel-solid: #ffffff;
      --panel-2: #f8fcff;
      --line: rgba(96, 165, 250, 0.14);
      --line-strong: rgba(96, 165, 250, 0.26);
      --text: #0f172a;
      --muted: #516277;
      --muted-2: #6f85a0;
      --blue: #60a5fa;
      --blue-soft: #dbeafe;
      --blue-soft-2: #eaf4ff;
      --blue-deep: #2563eb;
      --cyan-soft: #f0f9ff;
      --green: #10b981;
      --amber: #f59e0b;
      --red: #ef4444;
      --shadow: 0 16px 44px rgba(96, 165, 250, 0.14);
      --radius: 22px;
      --radius-sm: 14px;
      --max: 1380px;
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: Inter, Arial, Helvetica, sans-serif;
      background:
        radial-gradient(circle at top left, rgba(191, 219, 254, 0.45), transparent 28%),
        radial-gradient(circle at top right, rgba(219, 234, 254, 0.5), transparent 28%),
        linear-gradient(180deg, #fcfeff 0%, #f7fbff 52%, #eef7ff 100%);
      color: var(--text);
      min-height: 100vh;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .page-shell {
      position: relative;
      overflow: hidden;
    }

    .bg-orb {
      position: absolute;
      border-radius: 999px;
      filter: blur(90px);
      opacity: 0.5;
      pointer-events: none;
      z-index: 0;
    }

    .orb-1 {
      width: 260px;
      height: 260px;
      background: #dbeafe;
      top: 100px;
      left: -40px;
    }

    .orb-2 {
      width: 320px;
      height: 320px;
      background: #e0f2fe;
      top: 220px;
      right: -80px;
    }

    .orb-3 {
      width: 240px;
      height: 240px;
      background: #bfdbfe;
      bottom: 120px;
      left: 20%;
    }

    .container {
      max-width: var(--max);
      margin: 0 auto;
      padding: 0 22px;
      position: relative;
      z-index: 1;
    }

    .navbar {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(12px);
      background: rgba(255, 255, 255, 0.82);
      border-bottom: 1px solid rgba(96, 165, 250, 0.12);
    }

    .nav-inner {
      max-width: var(--max);
      margin: 0 auto;
      padding: 18px 22px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 14px;
      min-width: 0;
    }

    .brand-mark {
      width: 42px;
      height: 42px;
      border-radius: 14px;
      background: linear-gradient(135deg, #eff6ff, #dbeafe);
      border: 1px solid rgba(96, 165, 250, 0.2);
      display: grid;
      place-items: center;
      box-shadow: inset 0 0 12px rgba(96, 165, 250, 0.08);
      font-size: 1.05rem;
      font-weight: 800;
      color: var(--blue-deep);
    }

    .brand-title {
      font-size: 1rem;
      font-weight: 700;
      letter-spacing: 0.2px;
    }

    .brand-subtitle {
      color: var(--muted);
      font-size: 0.82rem;
      margin-top: 2px;
    }

    .hero {
      padding: 42px 0 28px;
    }

    .hero-panel {
      background: linear-gradient(135deg, rgba(255,255,255,0.98), rgba(240,249,255,0.98));
      border: 1px solid rgba(96, 165, 250, 0.16);
      border-radius: 30px;
      box-shadow: var(--shadow);
      overflow: hidden;
      position: relative;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 28px;
      padding: 34px;
    }

    .hero-left {
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      align-self: flex-start;
      padding: 8px 12px;
      border-radius: 999px;
      background: #eff6ff;
      border: 1px solid rgba(96, 165, 250, 0.16);
      color: var(--blue-deep);
      font-size: 0.82rem;
      font-weight: 600;
      letter-spacing: 0.2px;
    }

    .hero h1 {
      margin: 0;
      font-size: clamp(2.2rem, 5vw, 4rem);
      line-height: 1.05;
      letter-spacing: -0.03em;
      max-width: 760px;
    }

    .hero h1 .accent {
      background: linear-gradient(90deg, #60a5fa, #38bdf8, #2563eb);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero p {
      margin: 0;
      max-width: 760px;
      color: var(--muted);
      font-size: 1.03rem;
      line-height: 1.7;
    }

    .hero-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .hero-badge {
      padding: 10px 14px;
      border-radius: 999px;
      background: #ffffff;
      border: 1px solid rgba(96, 165, 250, 0.14);
      color: var(--muted);
      font-size: 0.9rem;
      box-shadow: 0 10px 24px rgba(96, 165, 250, 0.08);
    }

    .hero-right {
      display: grid;
      gap: 14px;
      align-content: start;
    }

    .mini-panel {
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(96, 165, 250, 0.14);
      border-radius: 22px;
      padding: 18px;
      box-shadow: 0 14px 34px rgba(96, 165, 250, 0.08);
    }

    .mini-panel h3 {
      margin: 0 0 8px 0;
      font-size: 1rem;
    }

    .mini-panel p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 0.94rem;
    }

    .search-shell {
      padding-bottom: 18px;
    }

    .search-panel {
      background: var(--panel);
      border: 1px solid rgba(96, 165, 250, 0.14);
      box-shadow: var(--shadow);
      border-radius: 28px;
      padding: 24px;
    }

    .search-form {
      display: grid;
      gap: 16px;
    }

    .search-row {
      display: grid;
      grid-template-columns: 1.8fr 0.8fr 0.8fr 0.9fr auto;
      gap: 12px;
      align-items: stretch;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .field label {
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--muted);
    }

    input[type="text"],
    select {
      width: 100%;
      padding: 14px 16px;
      border-radius: 16px;
      border: 1px solid rgba(96, 165, 250, 0.18);
      background: #fff;
      font-size: 0.98rem;
      color: var(--text);
      outline: none;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
      min-height: 50px;
    }

    input[type="text"]:focus,
    select:focus {
      border-color: rgba(37, 99, 235, 0.4);
      box-shadow: 0 0 0 4px rgba(96, 165, 250, 0.12);
    }

    .actions {
      display: flex;
      gap: 10px;
      align-items: end;
    }

    .btn {
      border: 0;
      cursor: pointer;
      padding: 14px 18px;
      border-radius: 16px;
      font-weight: 700;
      font-size: 0.95rem;
      min-height: 50px;
      transition: transform 0.16s ease, box-shadow 0.16s ease, opacity 0.16s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      background: linear-gradient(135deg, #60a5fa, #2563eb);
      color: white;
      box-shadow: 0 16px 24px rgba(37, 99, 235, 0.22);
    }

    .btn-secondary {
      background: #eff6ff;
      color: var(--blue-deep);
      border: 1px solid rgba(96, 165, 250, 0.16);
    }

    .quick-searches {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 8px;
    }

    .quick-link {
      padding: 10px 14px;
      border-radius: 999px;
      background: #f8fbff;
      border: 1px solid rgba(96, 165, 250, 0.14);
      color: var(--muted);
      font-size: 0.9rem;
      transition: all 0.18s ease;
    }

    .quick-link:hover {
      background: #eff6ff;
      color: var(--blue-deep);
      border-color: rgba(96, 165, 250, 0.22);
    }

    .results-shell {
      padding: 6px 0 56px;
    }

    .message {
      padding: 16px 18px;
      border-radius: 18px;
      margin-bottom: 18px;
      line-height: 1.65;
      border: 1px solid transparent;
    }

    .message-info {
      background: #eff6ff;
      color: #1e3a8a;
      border-color: rgba(96, 165, 250, 0.16);
    }

    .message-error {
      background: #fef2f2;
      color: #991b1b;
      border-color: rgba(239, 68, 68, 0.18);
    }

    .summary-card {
      background: #ffffff;
      border: 1px solid rgba(96, 165, 250, 0.12);
      box-shadow: var(--shadow);
      border-radius: 24px;
      padding: 22px;
      margin-bottom: 22px;
    }

    .summary-title {
      font-size: 0.92rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--blue-deep);
      font-weight: 700;
      margin-bottom: 10px;
    }

    .summary-text {
      color: var(--muted);
      line-height: 1.75;
      font-size: 0.98rem;
    }

    .result-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-bottom: 22px;
    }

    .meta-pill {
      background: #ffffff;
      border: 1px solid rgba(96, 165, 250, 0.12);
      border-radius: 999px;
      padding: 10px 14px;
      color: var(--muted);
      font-size: 0.9rem;
    }

    .section-grid {
      display: grid;
      gap: 20px;
    }

    .result-section {
      background: rgba(255, 255, 255, 0.98);
      border-radius: 26px;
      border: 1px solid rgba(96, 165, 250, 0.12);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .result-section.primary {
      border-color: rgba(37, 99, 235, 0.24);
      box-shadow: 0 18px 42px rgba(37, 99, 235, 0.16);
    }

    .section-head {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      padding: 22px 24px;
      border-bottom: 1px solid rgba(96, 165, 250, 0.1);
      background: linear-gradient(180deg, rgba(248, 252, 255, 0.96), rgba(255,255,255,0.96));
    }

    .section-title-wrap {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .section-icon {
      width: 40px;
      height: 40px;
      border-radius: 14px;
      display: grid;
      place-items: center;
      font-size: 1rem;
      background: #eff6ff;
      color: var(--blue-deep);
      border: 1px solid rgba(96, 165, 250, 0.14);
    }

    .section-title {
      font-size: 1.12rem;
      font-weight: 800;
      letter-spacing: -0.01em;
    }

    .section-subtitle {
      color: var(--muted);
      font-size: 0.92rem;
      margin-top: 4px;
    }

    .section-count {
      background: #eff6ff;
      color: var(--blue-deep);
      border-radius: 999px;
      padding: 10px 14px;
      font-weight: 700;
      font-size: 0.9rem;
      border: 1px solid rgba(96, 165, 250, 0.12);
      white-space: nowrap;
    }

    .cards {
      padding: 18px;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16px;
    }

    .card {
      background: linear-gradient(180deg, #ffffff, #fbfdff);
      border: 1px solid rgba(96, 165, 250, 0.12);
      border-radius: 22px;
      padding: 18px;
      box-shadow: 0 12px 28px rgba(96, 165, 250, 0.08);
      transition: transform 0.16s ease, border-color 0.16s ease, box-shadow 0.16s ease;
    }

    .card:hover {
      transform: translateY(-2px);
      border-color: rgba(96, 165, 250, 0.2);
      box-shadow: 0 18px 36px rgba(96, 165, 250, 0.12);
    }

    .card h3 {
      margin: 0 0 10px 0;
      font-size: 1.08rem;
      letter-spacing: -0.01em;
    }

    .card h3 a {
      color: var(--text);
    }

    .card h3 a:hover {
      color: var(--blue-deep);
    }

    .card p {
      margin: 6px 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 0.94rem;
    }

    .label {
      color: var(--text);
      font-weight: 700;
    }

    .card-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 12px;
    }

    .card-tag {
      padding: 8px 12px;
      border-radius: 999px;
      background: #f0f9ff;
      color: #0369a1;
      font-size: 0.82rem;
      font-weight: 600;
      border: 1px solid rgba(56, 189, 248, 0.12);
    }

    .empty-state {
      padding: 18px 24px 26px;
      color: var(--muted);
      line-height: 1.7;
    }

    .footer-space {
      height: 30px;
    }

    @media (max-width: 1100px) {
      .hero-grid,
      .search-row {
        grid-template-columns: 1fr;
      }

      .actions {
        align-items: stretch;
      }
    }

    @media (max-width: 820px) {
      .cards {
        grid-template-columns: 1fr;
      }

      .nav-inner {
        flex-direction: column;
        align-items: flex-start;
      }

      .section-head {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  
    .dual-tools {
      display: grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap: 18px;
      margin-top: 18px;
    }

    .tool-panel {
      background: rgba(255,255,255,0.82);
      border: 1px solid rgba(96, 165, 250, 0.14);
      border-radius: 18px;
      padding: 18px;
    }

    .tool-panel h3 {
      margin: 0 0 8px 0;
      font-size: 1rem;
    }

    .tool-panel p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 0.93rem;
    }

    .upload-row {
      display: flex;
      gap: 12px;
      align-items: end;
      flex-wrap: wrap;
      margin-top: 14px;
    }

    .upload-row input[type="file"] {
      flex: 1 1 280px;
      min-height: 46px;
      padding: 12px;
      border-radius: 14px;
      border: 1px dashed rgba(96, 165, 250, 0.3);
      background: #fff;
    }

    .signature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 12px;
      margin-top: 16px;
    }

    .signature-card {
      background: #fff;
      border: 1px solid rgba(96, 165, 250, 0.12);
      border-radius: 16px;
      padding: 14px;
    }

    .signature-label {
      font-size: 0.8rem;
      color: var(--muted-2);
      margin-bottom: 6px;
    }

    .signature-value {
      font-size: 1.05rem;
      font-weight: 700;
      color: var(--text);
    }

    .structure-match-list {
      display: grid;
      gap: 16px;
      margin-top: 18px;
    }

    .structure-match {
      background: var(--panel);
      border: 1px solid rgba(96, 165, 250, 0.14);
      border-radius: 20px;
      padding: 20px;
      box-shadow: 0 12px 28px rgba(96, 165, 250, 0.08);
    }

    .match-head {
      display: flex;
      justify-content: space-between;
      gap: 16px;
      align-items: start;
      flex-wrap: wrap;
    }

    .score-badge {
      display: inline-flex;
      padding: 10px 14px;
      border-radius: 999px;
      background: #eff6ff;
      color: var(--blue-deep);
      font-weight: 700;
      border: 1px solid rgba(96, 165, 250, 0.18);
    }

    .match-actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-top: 14px;
    }

    .tiny-note {
      color: var(--muted-2);
      font-size: 0.84rem;
      line-height: 1.6;
      margin-top: 10px;
    }

    @media (max-width: 980px) {
      .dual-tools {
        grid-template-columns: 1fr;
      }
    }

  </style>
</head>
<body>
  <div class="page-shell">
    <div class="bg-orb orb-1"></div>
    <div class="bg-orb orb-2"></div>
    <div class="bg-orb orb-3"></div>

    <div class="navbar">
      <div class="nav-inner">
        <div class="brand">
          <div class="brand-mark">MS</div>
          <div>
            <div class="brand-title">Molecular Search Tool</div>
            <div class="brand-subtitle">Binder-target intelligence for translational research</div>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
      <section class="hero">
        <div class="hero-panel">
          <div class="hero-grid">
            <div class="hero-left">
              <div class="eyebrow">Search Routing + Binder-First Exploration</div>
              <h1>Search across <span class="accent">targets, binders, trials,</span> and disease context</h1>
              <p>
                Exact target-style queries can go directly to a protein page, binder-style queries stay in a binder-first results flow,
                and disease-style queries become filtered exploration views.
              </p>
              <div class="hero-badges">
                <div class="hero-badge">EGFR → target page</div>
                <div class="hero-badge">EGFR binders → binder results</div>
                <div class="hero-badge">lung cancer → disease-tag filtered results</div>
              </div>
            </div>
            <div class="hero-right">
              <div class="mini-panel">
                <h3>Recommended searches</h3>
                <p>Try target names, binder names, disease terms, or clinical trial IDs to test the new routing behavior.</p>
              </div>
              <div class="mini-panel">
                <h3>Binder-first by default</h3>
                <p>Unless the search is an exact direct match for a specific page, the platform now keeps binders prominent in the results flow.</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="search-shell">
        <div class="search-panel">
          <form class="search-form" method="GET" action="/">
            <div class="search-row">
              <div class="field">
                <label for="q">Search</label>
                <input
                  id="q"
                  name="q"
                  type="text"
                  value="{{ query or '' }}"
                  placeholder="Try EGFR, EGFR binders, lung cancer, Cetuximab, or an NCT ID"
                />
              </div>

              <div class="field">
                <label for="binder_type">Binder Type</label>
                <select id="binder_type" name="binder_type">
                  <option value="">All binder types</option>
                  {% for item in filter_options.binder_types %}
                    <option value="{{ item }}" {% if selected_binder_type == item %}selected{% endif %}>{{ item }}</option>
                  {% endfor %}
                </select>
              </div>

              <div class="field">
                <label for="clinical_status">Clinical Status</label>
                <select id="clinical_status" name="clinical_status">
                  <option value="">All clinical statuses</option>
                  {% for item in filter_options.clinical_statuses %}
                    <option value="{{ item }}" {% if selected_clinical_status == item %}selected{% endif %}>{{ item }}</option>
                  {% endfor %}
                </select>
              </div>

              <div class="field">
                <label for="disease_name">Disease Filter</label>
                <select id="disease_name" name="disease_name">
                  <option value="">All diseases</option>
                  {% for item in filter_options.diseases %}
                    <option value="{{ item }}" {% if selected_disease_name == item %}selected{% endif %}>{{ item }}</option>
                  {% endfor %}
                </select>
              </div>

              <div class="actions">
                <button class="btn btn-primary" type="submit">Search</button>
                <a class="btn btn-secondary" href="/">Clear</a>
              </div>
            </div>

            <div class="quick-searches">
              <a class="quick-link" href="/?q=EGFR">EGFR</a>
              <a class="quick-link" href="/?q=EGFR+binders">EGFR binders</a>
              <a class="quick-link" href="/?q=lung+cancer">lung cancer</a>
              <a class="quick-link" href="/?q=HER2">HER2</a>
              <a class="quick-link" href="/?q=Cetuximab">Cetuximab</a>
            </div>
          </form>
          <div class="dual-tools">
            <div class="tool-panel">
              <h3>Sequence Similarity (Current MVP)</h3>
              <p>Paste a FASTA or amino-acid sequence into the main search bar above. Sequence-like input is automatically detected and routed through the sequence-style search behavior.</p>
            </div>
            <div class="tool-panel">
              <h3>Structure Similarity Search</h3>
              <p>Upload a PDB or mmCIF structure file to compare it against linked structures already stored in the platform.</p>
              <form method="POST" action="/structure-similarity" enctype="multipart/form-data">
                <div class="upload-row">
                  <input type="file" name="structure_file" accept=".pdb,.cif,.mmcif" required />
                  <button class="btn btn-primary" type="submit">Compare Structure</button>
                </div>
              </form>
              <div class="tiny-note">This beta MVP uses a structural signature based on atom count, residue count, chain count, spatial span, and radius of gyration. It does not use Foldseek or TM-score yet.</div>
            </div>
          </div>

        </div>
      </section>

      <section class="results-shell">
        {% if error %}
          <div class="message message-error">
            <strong>Error:</strong> {{ error }}
          </div>
        {% endif %}

        {% if route_hint %}
          <div class="message message-info">
            {{ route_hint }}
          </div>
        {% endif %}

        {% if summary %}
          <div class="summary-card">
            <div class="summary-title">Result Summary</div>
            <div class="summary-text">{{ summary }}</div>
          </div>
        {% endif %}

        {% if structure_error %}
          <div class="message message-error">
            <strong>Structure similarity error:</strong> {{ structure_error }}
          </div>
        {% endif %}

        {% if structure_similarity %}
          <div class="summary-card">
            <div class="summary-title">Structure Similarity Summary</div>
            <div class="summary-text">{{ structure_similarity.summary }}</div>
          </div>

          <div class="result-meta">
            <div class="meta-pill">Uploaded file: {{ structure_similarity.query_filename }}</div>
            <div class="meta-pill">Matches scored: {{ structure_similarity.matches|length }}</div>
            <div class="meta-pill">Candidate fetch failures: {{ structure_similarity.failure_count }}</div>
          </div>

          <div class="result-section primary" style="margin-bottom: 22px;">
            <div class="section-head">
              <div class="section-title-wrap">
                <div class="section-icon">S</div>
                <div>
                  <div class="section-title">Uploaded Structure Signature</div>
                  <div class="section-subtitle">Compact metadata extracted from the uploaded 3D structure file</div>
                </div>
              </div>
            </div>
            <div class="signature-grid">
              {% for key, value in structure_similarity.query_signature.items() %}
                <div class="signature-card">
                  <div class="signature-label">{{ key.replace('_', ' ')|title }}</div>
                  <div class="signature-value">{{ value }}</div>
                </div>
              {% endfor %}
            </div>
          </div>

          <div class="result-section primary">
            <div class="section-head">
              <div class="section-title-wrap">
                <div class="section-icon">M</div>
                <div>
                  <div class="section-title">Top Structure Matches</div>
                  <div class="section-subtitle">Ranked against linked PDB entries in your current dataset</div>
                </div>
              </div>
              <div class="section-count">{{ structure_similarity.matches|length }} result{% if structure_similarity.matches|length != 1 %}s{% endif %}</div>
            </div>

            {% if structure_similarity.matches %}
              <div class="structure-match-list">
                {% for item in structure_similarity.matches %}
                  <div class="structure-match">
                    <div class="match-head">
                      <div>
                        <h3 style="margin:0 0 8px 0;">{{ item.pdb_id or 'Linked Structure' }} · {{ item.structure_title or 'Untitled structure' }}</h3>
                        <p style="margin:0;"><span class="label">Protein:</span>
                          {% if item.protein_id %}
                            <a href="/protein/{{ item.protein_id }}">{{ item.gene_symbol or item.protein_name or 'Linked protein' }}</a>
                          {% else %}
                            {{ item.gene_symbol or item.protein_name or 'Not linked' }}
                          {% endif %}
                        </p>
                        <p><span class="label">Method:</span> {{ item.experimental_method or 'Not available' }}</p>
                        <p><span class="label">Resolution:</span> {{ item.resolution or 'Not available' }}</p>
                      </div>
                      <div class="score-badge">Similarity {{ item.similarity_score }}%</div>
                    </div>

                    <div class="signature-grid">
                      <div class="signature-card"><div class="signature-label">Atoms</div><div class="signature-value">{{ item.candidate_signature.atom_count }}</div></div>
                      <div class="signature-card"><div class="signature-label">Residues</div><div class="signature-value">{{ item.candidate_signature.residue_count }}</div></div>
                      <div class="signature-card"><div class="signature-label">Chains</div><div class="signature-value">{{ item.candidate_signature.chain_count }}</div></div>
                      <div class="signature-card"><div class="signature-label">Radius of Gyration</div><div class="signature-value">{{ item.candidate_signature.radius_gyration }}</div></div>
                    </div>

                    <div class="match-actions">
                      {% if item.viewer_url %}<a class="btn btn-secondary" target="_blank" href="{{ item.viewer_url }}">Open Viewer</a>{% endif %}
                      {% if item.external_url %}<a class="btn btn-secondary" target="_blank" href="{{ item.external_url }}">RCSB Page</a>{% endif %}
                    </div>

                    <div class="tiny-note">
                      Distance breakdown — atom: {{ item.score_breakdown.atom_diff }}, residue: {{ item.score_breakdown.residue_diff }}, chains: {{ item.score_breakdown.chain_diff }}, radius: {{ item.score_breakdown.rg_diff }}, span: {{ item.score_breakdown.span_diff }}, compactness: {{ item.score_breakdown.compactness_diff }}.
                    </div>
                  </div>
                {% endfor %}
              </div>
            {% else %}
              <div class="empty-state">No linked structures were comparable to the uploaded file.</div>
            {% endif %}
          </div>
        {% endif %}

        {% if results %}
          <div class="result-meta">
            <div class="meta-pill">Original query: {{ query }}</div>
            {% if effective_query and effective_query != query %}
              <div class="meta-pill">Search term used for matching: {{ effective_query }}</div>
            {% endif %}
            <div class="meta-pill">Intent: {{ results.intent }}</div>
            <div class="meta-pill">Primary section: {{ results.primary_section }}</div>
          </div>

          <div class="section-grid">
            {% for section_name in results.section_order %}
              {% set items = results[section_name] %}
              {% set is_primary = results.primary_section == section_name %}
              {% if section_name == 'binders' %}
                {% set title = 'Therapeutic Binders' %}
                {% set subtitle = 'Primary therapeutic objects and linked intervention molecules' %}
                {% set icon = 'B' %}
              {% elif section_name == 'proteins' %}
                {% set title = 'Protein Targets' %}
                {% set subtitle = 'Biological targets connected to binders and diseases' %}
                {% set icon = 'P' %}
              {% elif section_name == 'trials' %}
                {% set title = 'Clinical Trials' %}
                {% set subtitle = 'Validation and translational evidence layer' %}
                {% set icon = 'T' %}
              {% else %}
                {% set title = 'Disease Context' %}
                {% set subtitle = 'Disease tags and contextual disease records' %}
                {% set icon = 'D' %}
              {% endif %}

              <div class="result-section {% if is_primary %}primary{% endif %}">
                <div class="section-head">
                  <div class="section-title-wrap">
                    <div class="section-icon">{{ icon }}</div>
                    <div>
                      <div class="section-title">
                        {{ title }}
                        {% if is_primary %}
                          · Primary
                        {% endif %}
                      </div>
                      <div class="section-subtitle">{{ subtitle }}</div>
                    </div>
                  </div>
                  <div class="section-count">{{ items|length }} result{% if items|length != 1 %}s{% endif %}</div>
                </div>

                {% if items and items|length > 0 %}
                  <div class="cards">
                    {% if section_name == 'binders' %}
                      {% for item in items %}
                        <div class="card">
                          <h3><a href="/binder/{{ item.binder_id }}">{{ item.binder_name }}</a></h3>
                          <p><span class="label">Binder Type:</span> {{ item.binder_type or 'Not available' }}</p>
                          <p><span class="label">Clinical Status:</span> {{ item.clinical_status or item.approval_status or 'Not available' }}</p>
                          <p><span class="label">Modality:</span> {{ item.modality_name or 'Not available' }}</p>
                          <p><span class="label">Mechanism:</span> {{ item.mechanism_of_action or 'Not available' }}</p>
                          <div class="card-tags">
                            <span class="card-tag">Binder-first result</span>
                            {% if item.sequence %}
                              <span class="card-tag">Sequence available</span>
                            {% endif %}
                          </div>
                        </div>
                      {% endfor %}
                    {% elif section_name == 'proteins' %}
                      {% for item in items %}
                        <div class="card">
                          <h3><a href="/protein/{{ item.protein_id }}">{{ item.gene_symbol or item.protein_name }}</a></h3>
                          <p><span class="label">Protein Name:</span> {{ item.protein_name or 'Not available' }}</p>
                          <p><span class="label">Gene:</span> {{ item.gene_name or item.gene_symbol or 'Not available' }}</p>
                          <p><span class="label">UniProt:</span> {{ item.uniprot_accession or 'Not available' }}</p>
                          <p><span class="label">Organism:</span> {{ item.organism_name or 'Not available' }}</p>
                        </div>
                      {% endfor %}
                    {% elif section_name == 'trials' %}
                      {% for item in items %}
                        <div class="card">
                          <h3><a href="/trial/{{ item.trial_id }}">{{ item.nct_id or item.trial_title }}</a></h3>
                          <p><span class="label">Title:</span> {{ item.trial_title or 'Not available' }}</p>
                          <p><span class="label">Condition:</span> {{ item.condition_name or 'Not available' }}</p>
                          <p><span class="label">Phase:</span> {{ item.phase or 'Not available' }}</p>
                          <p><span class="label">Recruitment:</span> {{ item.recruitment_status or 'Not available' }}</p>
                        </div>
                      {% endfor %}
                    {% else %}
                      {% for item in items %}
                        <div class="card">
                          <h3><a href="/disease/{{ item.disease_id }}">{{ item.disease_name }}</a></h3>
                          <p><span class="label">Category:</span> {{ item.disease_category or 'Not available' }}</p>
                          <p><span class="label">Description:</span> {{ item.description or 'Not available' }}</p>
                          <div class="card-tags">
                            <span class="card-tag">Disease context</span>
                          </div>
                        </div>
                      {% endfor %}
                    {% endif %}
                  </div>
                {% else %}
                  <div class="empty-state">
                    No results were returned in this section for the current search and filter combination.
                  </div>
                {% endif %}
              </div>
            {% endfor %}
          </div>
        {% elif query %}
          <div class="message message-info">
            The search completed, but no records were returned.
          </div>
        {% endif %}
      </section>

      <div class="footer-space"></div>
    </div>
  </div>
</body>
</html>